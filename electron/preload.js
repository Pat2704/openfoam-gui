/**
 * OpenFOAM Studio — preload script.
 *
 * Runs in the renderer process with contextIsolation enabled. The Next.js
 * app runs as a normal web app and doesn't need any Node/Electron APIs
 * exposed to it, so this file is intentionally empty.
 *
 * Note: we deliberately do NOT touch document.title here — the page
 * <title> set by Next.js metadata ("OpenFOAM Studio - GUI") must remain
 * authoritative so the native window title is correct.
 */
